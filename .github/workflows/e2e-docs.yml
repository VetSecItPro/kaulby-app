name: E2E Documentation Updates

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  update-docs:
    uses: VetSecItPro/e2e-intelligence/.github/workflows/e2e-docs-template.yml@main
    with:
      repo-name: kaulby
      node-version: '20'
