name: TestID Scanner

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  scan:
    uses: VetSecItPro/e2e-intelligence/.github/workflows/testid-scanner-template.yml@main
    with:
      repo-name: kaulby
      min-priority: high
